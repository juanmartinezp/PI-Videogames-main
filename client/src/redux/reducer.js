import {
    GET_ALL_GAMES,
    GET_GAME_BY_ID,
    GET_GAME_BY_NAME,
    CREATE_GAME,
    GET_ALL_GENRES,
    SET_CURRENT_PAGE,
    CLEAR_FILTERS,
    ORDER_BY_NAME,
    ORDER_BY_RATING,
    ORDER_BY_LOCATION,
    ORDER_BY_GENRES,
} from './actions';

const initialState = {
    
};